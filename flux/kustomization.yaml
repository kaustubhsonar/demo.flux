apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: demo-flux
  namespace: flux-system
spec:
  interval: 5m0s
  path: ./flux
  prune: true
  sourceRef:
    kind: GitRepository
    name: demo-flux
    namespace: flux-system
  validation: client
  # optional: set concurrency or target namespace for Kustomize resources
