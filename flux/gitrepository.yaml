apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: demo-flux
  namespace: flux-system
spec:
  interval: 1m
  # Replace the URL below with the HTTPS/SSH URL of this repository
  url: https://github.com/kaustubhsonar/demo.flux.git
  ref:
    branch: main
  # If your repo is private, configure a Kubernetes Secret with SSH or HTTPS credentials
  # secretRef:
  #   name: git-credentials
