apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gitrepository.yaml
  - kustomization.flux.yaml
  - helmrelease-nginx.yaml
  # ... add any other manifest files that should be applied from this folder ...
